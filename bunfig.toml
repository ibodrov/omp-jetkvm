# Defense in depth for omp started from this directory. The werift dependency
# patch contains ICE UDP failures in every installed extension; this preload
# catches the identical stackless error if another Bun-native socket emits it
# before omp's fatal uncaught-exception handler.
preload = ["./scripts/filter-transient-socket.ts"]
